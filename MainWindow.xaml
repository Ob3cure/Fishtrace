<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Fishtrace.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Fishtrace"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Fishtrace"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>




        <!-- <Canvas Grid.Row="2">
            <Image x:Name="UtilsBar2" Source="Assets/80x80fish.svg" Height="100" Width="200" Canvas.Left="300" Canvas.Top="250"/>
        </Canvas> -->
        <!--
            The Frame hosts pages for your application content. Add your UI to
            MainPage.xaml rather than here so you can use Page features such as
            navigation events and the Loaded lifecycle.
        -->
        <TitleBar x:Name="AppTitleBar" Title="Fishtrace">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/80x80Fish.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
